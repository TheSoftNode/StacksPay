# Email Configuration Options

# Choose email provider: ethereal (dev), gmail, sendgrid, mailgun
EMAIL_PROVIDER=ethereal

# Gmail Configuration (use app password, not regular password)
# GMAIL_USER=your-email@gmail.com
# GMAIL_APP_PASSWORD=your-app-password

# SendGrid Configuration
# SENDGRID_API_KEY=your-sendgrid-api-key

# Mailgun Configuration
# MAILGUN_SMTP_USER=your-mailgun-smtp-user
# MAILGUN_SMTP_PASS=your-mailgun-smtp-password

# General SMTP Configuration (for custom SMTP servers)
# SMTP_HOST=smtp.your-provider.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-username
# SMTP_PASS=your-password
# SMTP_FROM=noreply@your-domain.com

# Frontend URL for email links
FRONTEND_URL=http://localhost:3000

# Email Rate Limiting (optional)
# EMAIL_RATE_LIMIT=5
# EMAIL_RATE_DELTA=20000
